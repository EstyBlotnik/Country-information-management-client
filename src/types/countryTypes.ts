export type CountryData = {
  _id: string;
  name: string;
  flag: string;
  population: number;
  region: string;
};
export type CountryWithoutID = {
  name: string;
  flag: string;
  population: number;
  region: string;
};
